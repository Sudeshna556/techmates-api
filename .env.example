# Copy this file to .env and fill in real values.
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster-host>/techMates
# any long random string, e.g. node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=
